---
id: TASK-FEAT-SRC-059-001-001
ssot_type: task
title: 实现 Fitness Rule Schema JSON 定义
status: draft
version: v1
parent_id: FEAT-SRC-059-001
derived_from_ids:
  - id: FEAT-SRC-059-001
    version: v1
    required: true
source_refs:
  - FEAT-SRC-059-001
  - TECH-FEAT-SRC-059-001
owner: governance
tags: [fitness, schema, p0, backend]
properties:
  src_root_id: SRC-059
  priority: P0
  estimated_effort: 2h
  tech_ref: TECH-FEAT-SRC-059-001
workflow_instance_id: wf_task_a485b35b
---

# TASK-FEAT-SRC-059-001-001: 实现 Fitness Rule Schema JSON 定义

## 任务描述

创建 `fitness_rule.schema.json` 文件，定义 Fitness Rule 的 JSON Schema。

## 输入

- FEAT-SRC-059-001 冻结 spec
- TECH-FEAT-SRC-059-001 技术设计文档
- JSON Schema Draft 2020-12 规范

## 工作内容

1. 创建文件 `spec/contracts/fitness-rule/v1/schema.json`
2. 实现以下核心字段定义：
   - `rule_id` (string, pattern: `^FIT-[A-Z0-9_-]+$`)
   - `rule_type` (enum: `hard_gate`, `quality_signal`)
   - `scan_command` (object: command, args, cwd, timeout_seconds)
   - `pass_criteria` (object with conditional schema)
   - `description` (string)
   - `dimension` (enum: 5 个 Fitness 维度)
   - `severity` (enum: blocker, major, minor, nit)
   - `evidence_binding` (object)
   - `metadata` (object)

## 输出

- `spec/contracts/fitness-rule/v1/schema.json` - JSON Schema 定义文件

## 验收标准

1. Schema 文件符合 JSON Schema Draft 2020-12 规范
2. 所有必需字段正确定义
3. `pass_criteria` 使用 `if/then/else` 条件 schema
4. 提供完整的字段描述和示例

## 依赖

- 无（这是 FEAT-SRC-059-001 的第一个任务）

## 完成定义 (DoD)

- [ ] Schema 文件创建
- [ ] Schema 自验证通过
- [ ] 代码审查通过
- [ ] 示例文件验证通过

---

*Generated by feat-to-delivery-prep workflow*
*Workflow Instance: wf_task_a485b35b*
*Parent: FEAT-SRC-059-001*
