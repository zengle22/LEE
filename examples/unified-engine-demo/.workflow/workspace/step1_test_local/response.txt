好的，我将为您执行本地 Engine 接口测试并生成测试报告。

### 测试计划
1. **环境检查**：确认本地服务 `http://127.0.0.1:8045/v1` 是否存取。
2. **模型列表测试**：调用 `/v1/models` 接口，验证服务是否响应以及支持的模型。
3. **对话补全测试**：调用 `/v1/chat/completions` 接口，验证核心推理功能。
4. **生成报告**：汇总测试结果。

首先，我将编写一个测试脚本。

