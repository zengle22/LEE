# 修复 Claude 官方登录配置

## 问题
CC Switch 错误地为"官方登录"配置了本地代理 base_url，导致无法正常使用官方 API。

## 修复步骤

### 方法 1：在 CC Switch 中修复（推荐）

1. **打开 CC Switch**

2. **编辑"官方登录"供应商**：
   - 找到"官方登录"或您正在使用的供应商
   - 点击编辑按钮
   - **重要**：将 "Base URL" 字段**完全清空**（删除所有内容）
   - 如果有 API Key 字段，也清空（官方登录不需要）
   - 保存

3. **启用该配置**

4. **重启 Claude Code**

### 方法 2：手动修改配置文件

如果 CC Switch 无法正常修改，手动修复：

1. **打开配置文件**：
   ```
   C:\Users\shado\.claude\settings.json
   ```

2. **修改为以下内容**：
   ```json
   {
     "enabledPlugins": {
       "glm-plan-bug@zai-coding-plugins": true,
       "glm-plan-usage@zai-coding-plugins": true
     },
     "permissions": {
       "allow": [
         "mcp__pencil"
       ]
     }
   }
   ```

   **关键点**：
   - 删除整个 `"env"` 部分
   - 不要设置 `ANTHROPIC_BASE_URL`
   - 不要设置 `ANTHROPIC_AUTH_TOKEN`

3. **保存文件**

4. **重启 Claude Code**

5. **在浏览器中完成官方登录**

### 方法 3：使用 CC Switch 的"恢复官方登录"功能

如果 CC Switch 有"恢复官方登录"选项：
1. 在 CC Switch 中找到"官方登录"预设
2. 点击"恢复默认"或"重置"
3. 启用该配置
4. 重启 Claude Code

---

## 官方登录的正确流程

官方登录**不需要**配置 API Key 或 base_url：

1. **确保配置文件中没有代理设置**
   - 不设置 `ANTHROPIC_BASE_URL`
   - 不设置 `ANTHROPIC_AUTH_TOKEN`

2. **启动 Claude Code**

3. **浏览器会自动打开官方登录页面**

4. **完成 OAuth 授权**

---

## 验证修复

修复后，您的 `settings.json` 应该**不包含**以下内容：
- ❌ `"ANTHROPIC_BASE_URL"`
- ❌ `"ANTHROPIC_AUTH_TOKEN"`

正确的配置文件示例：
```json
{
  "enabledPlugins": {
    "glm-plan-bug@zai-coding-plugins": true,
    "glm-plan-usage@zai-coding-plugins": true
  },
  "permissions": {
    "allow": [
      "mcp__pencil"
    ]
  }
}
```

---

## 如果还是报错

如果上述方法都无效，可能需要：

1. **完全卸载 CC Switch**
2. **手动清理配置**：
   ```
   删除 C:\Users\shado\.claude\settings.json
   删除 C:\Users\shado\.cc-switch\ (可选)
   ```
3. **重新安装 CC Switch**
4. **选择正确的官方登录预设**（确保 base_url 为空）
