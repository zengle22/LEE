---
id: TESTSET-FEAT-SRC-059-001
ssot_type: test_set
title: Fitness Rule Schema 验证测试集
status: draft
version: v1
parent_id: FEAT-SRC-059-001
derived_from_ids:
  - id: FEAT-SRC-059-001
    version: v1
    required: true
source_refs:
  - FEAT-SRC-059-001
  - TECH-FEAT-SRC-059-001
owner: qa
tags: [fitness, schema, validation, qa]
properties:
  src_root_id: SRC-059
  priority: P0
  test_type: unit_and_integration
workflow_instance_id: wf_task_a485b35b
---

# TESTSET-FEAT-SRC-059-001: Fitness Rule Schema 验证测试集

## 测试集目标

验证 Fitness Rule Schema 定义和验证工具的正确性。

## 测试范围

1. Schema 文件自验证
2. 合法 YAML 文件验证
3. 合法 JSON 文件验证
4. 非法文件检测
5. 错误输出格式

---

## 测试用例

### TC-001: Schema 文件自验证

| 字段 | 内容 |
|------|------|
| **目的** | 验证 schema 文件本身符合 JSON Schema Draft 2020-12 规范 |
| **前置条件** | schema 文件已创建 |
| **步骤** | 1. 使用 jsonschema 库验证 schema 文件自身 |
| **期望结果** | Schema 自验证通过 |
| **trace_hints** | ["TASK-FEAT-SRC-059-001-001"] |

### TC-002: 合法 YAML 文件验证通过

| 字段 | 内容 |
|------|------|
| **目的** | 验证合法的 YAML 格式 fitness rule 文件验证通过 |
| **前置条件** | 准备符合 schema 的 YAML 示例文件 |
| **步骤** | 1. 执行 `validate_fitness_rule.py test_fitness_rule.yaml` |
| **期望结果** | 返回 0，输出 "PASS" |
| **trace_hints** | ["TASK-FEAT-SRC-059-001-002"] |

### TC-003: 合法 JSON 文件验证通过

| 字段 | 内容 |
|------|------|
| **目的** | 验证合法的 JSON 格式 fitness rule 文件验证通过 |
| **前置条件** | 准备符合 schema 的 JSON 示例文件 |
| **步骤** | 1. 执行 `validate_fitness_rule.py test_fitness_rule.json` |
| **期望结果** | 返回 0，输出 "PASS" |
| **trace_hints** | ["TASK-FEAT-SRC-059-001-002"] |

### TC-004: 缺失必需字段检测

| 字段 | 内容 |
|------|------|
| **目的** | 验证缺失必需字段时被正确检测 |
| **前置条件** | 准备缺失 `rule_id` 字段的测试文件 |
| **步骤** | 1. 执行 `validate_fitness_rule.py invalid_missing_field.yaml` |
| **期望结果** | 返回 1，错误信息包含 "rule_id" 和 "required" |
| **trace_hints** | ["TASK-FEAT-SRC-059-001-002"] |

### TC-005: rule_type 枚举验证

| 字段 | 内容 |
|------|------|
| **目的** | 验证 rule_type 只能是 hard_gate 或 quality_signal |
| **前置条件** | 准备 `rule_type: invalid_type` 的测试文件 |
| **步骤** | 1. 执行 `validate_fitness_rule.py invalid_rule_type.yaml` |
| **期望结果** | 返回 1，错误信息包含 "enum" |
| **trace_hints** | ["TASK-FEAT-SRC-059-001-002"] |

### TC-006: pass_criteria 条件 schema 验证

| 字段 | 内容 |
|------|------|
| **目的** | 验证 pass_criteria 的条件分支正确性 |
| **前置条件** | 准备 `kind: exit_code` 但缺少 `exit_code` 字段的测试文件 |
| **步骤** | 1. 执行 `validate_fitness_rule.py invalid_pass_criteria.yaml` |
| **期望结果** | 返回 1，错误信息包含条件验证失败 |
| **trace_hints** | ["TASK-FEAT-SRC-059-001-002"] |

---

## 执行策略

| 阶段 | 测试用例 | 执行频率 |
|------|----------|----------|
| 开发阶段 | TC-001 ~ TC-006 | 每次代码修改后 |
| CI 阶段 | TC-001, TC-002, TC-003, TC-004 | 每次提交 |
| 回归测试 | 全部 | 每周 |

---

## 通过标准

- 所有测试用例 100% 通过
- 无假阳性（合法文件被误判为非法）
- 无假阴性（非法文件被误判为合法）

---

*Generated by QA Test Set Production Workflow*
*Parent: FEAT-SRC-059-001*
