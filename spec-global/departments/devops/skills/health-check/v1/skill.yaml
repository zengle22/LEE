# DevOps Health Check Skill v1.0

kind: skill
version: 1.0

id: skill.devops.health_check
name: Health Check
description: >
  执行健康检查。
owner: devops-team
tags: [devops, deploy]

interface:
  inputs:
    type: object
    required: [env]
    properties:
      env:
        type: string
  outputs:
    type: object
    properties:
      result_path:
        type: string
