# Migrated by lint_l3_templates.py on 2026-03-15T00:01:44.029644
kind: l3_workflow_template
version: '1.0'
id: template.dev.evidence_pack_l3
name: Evidence Pack L3 Template
description: 'Canonical L3 workflow for collecting, reviewing, and packaging delivery

  evidence before the smoke gate closes the feature workflow.

  '
owner: development
tags:
- template
- dev
- evidence
- l3
- canonical
- adr-008
contracts:
  shared_input_schema: ../../../../../spec/contracts/shared-input-schema/v1/schema.json
  stage_definition: ../../stages/l3-evidence-pack.yaml
  input_schema: ../../contracts/evidence-pack/v1/schema.json
  integration_spec: ../../contracts/evidence-pack/v1/integration-spec.md
instance_schema:
  required_fields:
  - id
  - template_id
  - parent_phase_id
  - repo_id
  - formal_ssot_id
  - source_refs
  - governing_adrs
  - integration_outputs
  - verification_results
  output_fields:
  - evidence_index_ref
  - verification_summary_ref
  - evidence_pack_ref
  - delivery_candidate_ref
  - pack_review_ref
  - smoke_gate_inputs
stages:
- id: execution
  name: Execution Pipeline
  kind: stage
  description: Auto-migrated from root-level steps
  steps:
  - id: collect_evidence
    name: Collect Evidence
    kind: agent
    description: Gather implementation refs, integration outputs, and review evidence.
    agent_id: agent.dev.bug_fix_verifier
    mandatory: true
    depends_on: []
    outputs:
    - evidence_index_ref
    - verification_inputs
  - id: audit_evidence
    name: Audit Evidence
    kind: agent
    description: Review evidence completeness against the acceptance and governance baseline.
    agent_id: agent.dev.code_self_reviewer
    mandatory: true
    depends_on:
    - collect_evidence
    outputs:
    - verification_summary_ref
    - coverage_gap_ref
  - id: package_evidence
    name: Package Evidence
    kind: agent
    description: Produce the formal evidence pack passed to the smoke gate.
    agent_id: agent.dev.bug_fix_verifier
    mandatory: true
    depends_on:
    - audit_evidence
    outputs:
    - evidence_pack_ref
    - delivery_candidate_ref
  - id: review_package
    name: Review Evidence Pack
    kind: agent
    description: Perform a final review on the packaged evidence before gate handoff.
    agent_id: agent.dev.code_self_reviewer
    mandatory: true
    depends_on:
    - package_evidence
    outputs:
    - pack_review_ref
    - smoke_gate_inputs
