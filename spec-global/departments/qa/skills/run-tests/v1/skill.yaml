# QA Run Tests Skill v1.0

kind: skill
version: 1.0

id: skill.qa.run_tests
name: QA Run Tests
description: >
  执行回归测试脚本。
owner: qa-team
tags: [qa, test]

interface:
  inputs:
    type: object
    required: [scripts_dir]
    properties:
      scripts_dir:
        type: string
        description: "测试脚本目录"
  outputs:
    type: object
    properties:
      result_path:
        type: string
